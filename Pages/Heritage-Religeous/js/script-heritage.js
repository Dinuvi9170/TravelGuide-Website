function changeCardColor(card) {
    card.style.backgroundColor = "#c6c6c6"; // Change the card color to blue on mouseover
  }
  
  function resetCardColor(card) {
    card.style.backgroundColor = " #ffffff "; // Reset the card color to the original color on mouseout
  }
